# Resend (required for quote & contact forms to send email)
# Get your API key at https://resend.com — without this, "Submit quote request" will show an error.
RESEND_API_KEY=re_xxxxxxxxxxxx
# Sender address: use a domain you verified in Resend (e.g. noreply@yourdomain.com), or leave unset to use Resend's default.
RESEND_FROM_EMAIL=noreply@yourdomain.com
# Quote catalog is sent FROM this address TO the customer's email (via Gmail SMTP)
QUOTE_FROM_EMAIL=younusali6030@gmail.com
# Where to receive quote notification emails (details + CSV attachment). Default: younusali6030@gmail.com
QUOTE_NOTIFY_EMAIL=younusali6030@gmail.com
# Gmail App Password for the account above (create at myaccount.google.com/apppasswords)
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# Optional: append each quote to a Google Sheet (like one Excel file that keeps growing)
# 1) Create a Google Sheet, share it with the service account email (Editor). 2) Get sheet ID from URL: docs.google.com/spreadsheets/d/SHEET_ID/edit
# GOOGLE_SHEET_ID=your_sheet_id
# Optional: separate Google Sheet just for post-purchase form submissions
# If not set, post-purchase will use GOOGLE_SHEET_ID above.
# GOOGLE_PURCHASE_SHEET_ID=your_post_purchase_sheet_id
# GOOGLE_SERVICE_ACCOUNT_EMAIL=xxx@xxx.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional: live tentative rates in catalog (search Indore/Siyaganj and extract from results)
# Serper: https://serper.dev — get API key for Google search (free tier: 2500 queries)
# SERPER_API_KEY=
# OpenAI: for extracting supplier/rates from search snippets (uses gpt-4o-mini)
# OPENAI_API_KEY=

# Where to receive contact form submissions (optional; default: younusali6030@gmail.com)
CONTACT_TO_EMAIL=younusali6030@gmail.com
# Quote notification emails (optional; default: younusali6030@gmail.com)
QUOTE_TO_EMAIL=shoebali786@gmail.com

# Public site URL (for sitemap, robots, Open Graph)
NEXT_PUBLIC_SITE_URL=https://hmibrahimco.com

# Google Analytics 4 — full reports: page views, product views, quote/contact submissions, call/WhatsApp clicks
# Copy this line to .env.local and add the same in Vercel → Project → Settings → Environment Variables
NEXT_PUBLIC_GA_ID=G-0XV7YK12T2

# (Optional) Simple admin password for /admin routes (not used for post-purchase Google Sheet storage)
# ADMIN_PASSWORD=changeme
